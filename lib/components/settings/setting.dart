/// 애플리케이션의 개별 설정을 관리하기 위한 추상 클래스입니다.
///
/// 이 클래스를 구현하여 특정 설정 항목(예: 테마, 알림)에 대한
/// 값의 저장 및 조회를 표준화할 수 있습니다.
abstract class Setting<T> {
  /// 저장된 현재 설정 값을 불러와 반환합니다.
  T getValue();

  /// 설정을 주어진 값으로 업데이트하고 영구적으로 저장합니다.
  void setValue(T value);
}
